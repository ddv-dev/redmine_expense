<%
  settings = Setting.plugin_redmine_expense
  status_in_progress = settings['status_in_progress'].to_a
%>

<script>
  window.expenseSettings = {
    statusInProgress: <%= status_in_progress.to_json.html_safe %>
  };
  console.log('Expense settings loaded:', window.expenseSettings);
</script>
